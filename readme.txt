AI Handwritten Digit Recognition using Streamlit

This project uses a simple neural network trained on the MNIST dataset to recognize handwritten digits (0–9). 
Built using Streamlit and TensorFlow, it allows you to upload an image of a digit and get instant AI-based prediction.

How to run:
1. Open terminal in this folder.
2. Run: pip install -r requirements.txt
3. Run: streamlit run app.py
4. The app opens in your browser.

